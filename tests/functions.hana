/*
x = "a"
function outer() begin
    x = "b"
    function inner() begin
        x
    end
    inner()
    "outer " + x // wtf? x => GET LOCAL UP 0 1?
end

outer()
"global " + x
*/

/*
function a() begin
    function b() begin
    end

    // b isn't free?

end
a()
*/


function adder(n) begin
    function inner(x) begin
        return n + x
    end
    return inner
end

x = adder(5)
x(10)
