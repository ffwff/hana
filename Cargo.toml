[package]
name = "haru"
version = "0.1.0"
authors = ["ffwff <mogamiriver@tuta.io>"]
edition = "2018"
build = "build.rs"

[dependencies]
decorator = { path = "./src/decorator/" }
libc = "0.2.0"
ansi_term = "0.11"
codemap = "0.1"

[build-dependencies]
peg = "0.5"
cc = { version = "1.0", features = ["parallel"] }

[profile.release]
lto = true